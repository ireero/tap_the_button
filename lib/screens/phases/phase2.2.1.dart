import 'package:flutter/material.dart';

class PhaseTwoTwoOne extends StatelessWidget {
  const PhaseTwoTwoOne({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Icon(Icons.block, color: Colors.black),
        backgroundColor: Colors.red,
      ),
      body: const Center(child: Text('Not Here :(')),
    );
  }
}
